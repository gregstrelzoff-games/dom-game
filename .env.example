# ===== REQUIRED IN PROD =====
# External URL of your app (used for email links)
APP_ORIGIN=https://your-domain.com

# Admin API token (used by /admin UI)
ADMIN_TOKEN=change-me

# ===== SMTP (for real emails) =====
# If omitted, emails fall back to a dev outbox visible in /admin
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASS=your_api_key
SMTP_FROM="Game Team" <no-reply@your-domain.com>
